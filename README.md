# Simple LAMP Stack
This repository is a simple lamp stack to test and use as reference. 

## Important Notes
* `docker-compose up -d --build` to start docker compose and build images.
* To access the application: 
    * Web Application: http://localhost:8080
    * phpMyAdmin: http://localhost:8081